import { NotificationModel } from './notification.model';

export class NotificationMapModel {
    totalWorkspace: number;
    totalChannel: number;
    totalDirect: number;
    notifications: NotificationModel[];
}